schemaVersion: 1
# Render Blueprint config to automatically handle React SPA routes
services:
  - type: web
    name: grit-credit
    env: static
    buildCommand: npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
