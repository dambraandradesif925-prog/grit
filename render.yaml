schemaVersion: 1
# Render Blueprint config to serve React SPA via Express server
services:
  - type: web
    name: grit-credit
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
